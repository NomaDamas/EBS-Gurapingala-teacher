{
  "name": "ebs-gurapingala-teacher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "eval": "node scripts/run-eval.js",
    "readiness": "node scripts/readiness-audit.js",
    "smoke": "node scripts/smoke-worker.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.24.3"
  }
}
